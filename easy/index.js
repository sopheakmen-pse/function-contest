function Product(ProductA, ProductB, ProductC) {
    let priceA = 3;
    let priceB = 5;
    let priceC = 6;

    let totalprice = (ProductA * priceA) + (ProductB * priceB) + (ProductC * priceC);

    return totalprice;
}

